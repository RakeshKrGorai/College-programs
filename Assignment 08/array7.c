//Insert element at a given index
#include<stdio.h>
int main()
{
    int arr[]={10,22,45,68,98};
    int index, num, i, n=10;
    printf("Enter number to be inserted : ");
    scanf("%d", &num);
    printf("Enter index (0-9) : ");
    scanf("%d", &num);
    for(i=0;i<n;i++){
        if(i==index){
            
        }
    }
    return 0;
}
